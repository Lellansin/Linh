property.txt

判断 __updated__ 字段为 true 才会再次运行属性方法

只执行一次

a is [3,4,5,8,12]
print (max of a add min of a)

property max of list
-----------------------
	max is 0
	for (var item in list)	
		if item > max
			max is item
	return max



property max, min of list
-----------------------
	max is 0
	min is 100
	for (var item in list)	
		if item > max
			max is item
		if item < min
			min is item
	return (max , min)


