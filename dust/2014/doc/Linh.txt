

一个文件就是一个类.

类似 node require 导出 (可以查看导出的头文件) 自动生成头文件.

头文件重名




检查强转 信息 丢失.

句子 ; , 段落 {} , 式子 ()


类似数学的表达式, 可以查看单位


发散思维的界面模式

按照流程图的形式编程

自动方法, 隐藏在函数开头或者结尾, 自动执行的函数方法

一层循环.终止

查看函数和变量的被调用。


每个函数都要注册, 在访问其他函数的时候需要将函数id一同传入.


设置变量以及函数的访问权限 

if(函数名.access == 访问.access) return;


访问日志记录(调试才有)


指定要处理的数据格式以及示例, 函数可以根据数据示例进行模型演算.

指定演算数据, 演算日志 (assert)




编译时, 命名空间


解析文本

1, 判断是否为关键字
2, 判断是否为常量(数字, 字符, 字符串)
3, 判断是否为变量, 判断中文
4, 未定义的变量



导出变量, 函数

export.var = []

函数也是一个值, 也可以导出





如果 x 在 list 中是最大值
if x of list is max ----> max is a property

typedef list is int array;

property function
---------------------
property max of list
{
	for(list)
		if
		    > max

    return value of max;
    return reference of max; // auto define you var into malloc
}

function sort of list
{
	param x, y, z;

	list == this; // not use var self = this;

	for(var i = 0; i < list.length; i++) {
		...
	}
}


while param a, b, c call sort of list
while param a, b, c sort the list a

sort a while param is a, b, c 
sort list a while param is a, b, c  // check type
sort my list a while param is a, b, c  // check scope

for (var i is a.index) {
	print a[i];
	print (index i of a);
}



<function> <scope> <type> <var> while param is <var, ...>


globel

作用域
scope.list
scope.var.list


the max of list
the max in list i








自动执行列表

类型的
变量的

变量的自动执行列表是临时的

自动执行列表默认包括
创建
赋值
访问
拷贝
剪切
更改
增加
销毁
自定义销毁时间


权限列表

读
写
执行   函数调用



权限列表

继承
重写
调用
复写

访问，属性，属性函数调用权限
调用，方法


any var has auto property list, access list.






not important 支持 json 字符串




what I want to do?
todolist


what is <var>?
what is <function>?
what is <object>?

what  ----> call the type of <var>
how   ----> 
when  ----> call the run time
where ----> call the place property



calling


every var has property is:

noun

* access
* auto
* type   ----> what
* place  ----> where
* size   ----> how many size of <var>
* status ----> when








Formula ()
Sentence ;
Block {}


if else

if [ sentence (formula) ] :
	...
else
	...


swith [ sentence (formula) ]
	case
	


循环

a is [1,2,3,4]

数组就是两个 array
一个存地址, 一个存类型

输出时, 根据类型来调用输出方法.

list is [3,4,2,5]

for (var item in list)
	print item 			# call _value_
	print item.__type__




private key word

function sort of list
-----------------------
	private a
