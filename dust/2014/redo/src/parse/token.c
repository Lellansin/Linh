#include "token.h"

typedef struct
{
    int length;
    char *word;
} token_t;

int token(char const *sentence)
{
    char *start;
    
    // 1. check brackets

    // 2. start++, end--;

    // 3. split with space

    return 0;
}