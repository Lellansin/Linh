
#include "parser.h"

int parse(int argc, char const *argv[])
{
	/* 将一段字符串，比如 (print "hello world")
		处理为 
		token[0] print
		token[1] "hello world"

		如 (print (add 1 2))
		处理为 
		token[0] print
		token[1] (add 1 2)

	*/

	return 0;
}